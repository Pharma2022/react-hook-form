import { Controller } from "react-hook-form";

export function RHFAutocomplete(){
    // return <Controller control={control} name={name} render={(params)=>(<Autocomplete )}/>
    <div>Reach Hook Form</div>
}